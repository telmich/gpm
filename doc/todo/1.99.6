done:
- have a look at make dist: (in 1.20.3pre6 already)
   - make release
   - make test - always (build)
- cleanup autoconf stuff
- add commit to version, if git version is exiting:
   git-show --abbrev-commit
   -x git
   -d .git
